#include<simplecpp>
main_program{
	turtleSim();
	repeat(5){
		forward(170);//The length of the side of star
		right(144);// Angle to rotate with
	}
	wait(5);
	/*Here the main concept is the polygon that forms in between is to be considered and use geometry
	  */
}

